# Prisma / SQLite — local database file
DATABASE_URL="file:./dev.db"

# OpenAI — required for AI features (prioritization, decomposition)
OPENAI_API_KEY=

# Optional: override the default model
# OPENAI_MODEL=gpt-4o-mini
